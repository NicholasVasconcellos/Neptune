// Centralized logo export.
// app.json also references this asset directly as "./assets/neptune-logo-no-bg.png"
// since it cannot use JS imports — update both places when changing the logo.
export const AppLogo = require("@/assets/neptune-logo-no-bg.png");
